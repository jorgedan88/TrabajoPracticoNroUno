@model List<Movie>


    
<div class="text-center" class="border border-dark">
    <h1 class="display-4 border-dark container mt-5 bg-primary text-white">Peliculas</h1>
    <p class="p-3 mb-2 bg-body text-white">Top 10 mejores peliculas de Herramientas de Programacion</a>.</p>
</div>

<ol>
    @foreach (var item in Model)
    {
        <li><a asp-action="Detail" asp-route-id="@item.Code">@item.Name</a></li>
    }
</ol>

<a asp-action="Create" class="btn btn-primary">Agregar Pelicula</a>

